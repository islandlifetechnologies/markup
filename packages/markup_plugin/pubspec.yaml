name: markup_plugin
description: Contains common code utilized for the markup packages.
homepage: https://github.com/islandlifetechnologies/markup/tree/main/packages/markup_plugin
version: 1.0.0

resolution: workspace

environment:
  sdk: ^3.13.0


dependencies:
  markup_common: ^1.0.0

dev_dependencies:
  build_runner: ^2.16.0
  embed: ^1.6.6
  ilt_lints: ^1.0.1
  json_serializable: ^6.14.1
  test: ^1.31.2
