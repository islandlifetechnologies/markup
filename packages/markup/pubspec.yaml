name: 'markup'
description: 'Automated processing for Markdown files to simplify documentation creation and maintenance.'
homepage: https://github.com/islandlifetechnologies/markup/tree/main/packages/markup
version: 1.0.0-alpha0

resolution: workspace

environment:
  sdk: ^3.13.0

executables: 
  markup: markup

dependencies:
  glob: ^2.1.3
  json_annotation: ^4.12.0
  markup_common: ^1.0.0
  meta: ^1.16.0
  path: ^1.9.1

  embed_annotation: any
dev_dependencies:
  build_runner: ^2.16.0
  embed: ^1.6.6
  ilt_lints: ^1.0.1
  json_serializable: ^6.14.1
  test: ^1.31.2
