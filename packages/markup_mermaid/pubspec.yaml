name: markup_mermaid
description: Plugin to process Mermaid diagrams within a markup document.
homepage: https://github.com/islandlifetechnologies/markup/tree/main/packages/markup_mermaid
version: 1.0.0

resolution: workspace

environment:
  sdk: ^3.13.0

executables: 
  markup_mermaid: markup_mermaid

dependencies:
  embed_annotation: ^1.2.3
  markup_common: ^1.0.0
  path: ^1.9.1

dev_dependencies:
  build_runner: ^2.16.0
  embed: ^1.6.6
  ilt_lints: ^1.0.1
  json_serializable: ^6.14.1
  markup: ^1.0.0
  test: ^1.31.2
